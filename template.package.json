{
    "name": "@bunstacks/codepark-workspace",
    "type": "module",
    "main": "./src/codepark.js",
    "scripts": {
        "dev": "bun run --watch ./src/codepark.js --mode=dev --cwd=$CODEPARK_CWD",
        "serve": "bun run --watch ./src/codepark.js --mode=ssr --cwd=$CODEPARK_CWD"
    },
    "peerDependencies": {
        "typescript": "dev"
    }
}